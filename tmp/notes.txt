
	levelfile.bin	
		|
		V
	levelBinaryParser
		|
		|
		V
	output the level file data
	and save the file's data
	into `byte[] data`
		|
		|				we don't care about hitboxes yet
		V						Λ
	parse the values from byte[] data			|
	so to spawn specific objects to the -----> shown to PlayScreen
	screen using IDs and x-y coords


x1 x2 x3 == 16^2 16^1 16^0 or == xx xx xx, for example 1d 2a 33 == 1911347
x1 x2 x3 ==> one singular x coord. that would probably require bit shifting

since it read from left to right, said our first object have 01 00 00 C8 00 00 FF 00
with format as: id x1 x2 x3 y1 y2 y3 par (parameter): 
- data[32] = object 1
- data[40] = object 2
- data[48] = object 3
etc... as long the format is data[24+n*8] = object n
so again it read from left to right => data[33] = object 1's x1; data[34] = object 1's x2 and data[35] = object 1's x3 values

how would you combine all these values together, probably: byte x = 0x00x1x2x3 = (00 << 24) + (x1 << 16) + (x2 << 8) + x3, same for byte y = 0x00y1y2y3 = (00 << 24) + (y1 << 16) + (y2 << 8) + y3

anyways after that we need to print it onto the screen by grabbing its value and place it inside these g.drawRegion(); or some like that